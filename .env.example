# Copy to .env and `export $(cat .env | xargs)` before running scripts,
# or use a tool like direnv / python-dotenv.
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=research_pulse
